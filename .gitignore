# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Brainstorm + scratch
.superpowers/

# Reference clones (local research only — never commit)
.refs/

# Model weights (never commit)
*.safetensors
*.gguf
*.bin
*.pt
*.pth

# HF / model cache + diffusers-local layout
models/

# Gradio runtime
gradio_cached_examples/
output/
flagged/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# CI workflow retained locally; pushing it needs a PAT with the "workflow" scope
.github/workflows/

# Playwright MCP artifacts + local test assets
.playwright-mcp/
